= content_for :title, t('models.polls.actions.new.title')

.row
  .col-sm-6.col-sm-offset-3
    %h1= t('models.polls.actions.new.title')

    = render 'form'

    = link_to t('models.polls.navigation.back'), place_polls_path(@place), class: 'btn btn-default'
