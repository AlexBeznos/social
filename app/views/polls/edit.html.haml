%h1 Editing poll

= render 'form'

= link_to 'Show', place_poll_path(id: @poll.id, place_id: @place.slug)
\|
= link_to 'Change answers', place_poll_answers_path(@place, @poll)
\|
= link_to 'Back', place_polls_path(@place)
