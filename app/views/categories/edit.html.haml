= content_for :title, t('models.categories.actions.edit.title')
= content_for :navigation, 'false'

.row
  .col-sm-6.col-sm-offset-3
    %h1= t('models.categories.actions.edit.title')

    = render 'form'

    = render 'categories/message_form/new'