= content_for :title, "Edit Message"
%section.content-header
  %h1= yield(:title)

  - navigation_add 'Users', adm_users_path
  - navigation_add "User #{@message.place.user.id}", adm_user_path(@message.place.user)
  - navigation_add "Place #{@message.place.id}", adm_place_path(@message.place)
  - navigation_add "Edit message", edit_adm_message_path(@message)
  = render_navigation

%section.content
  .row
    .col-md-6
      .box
        .box-body= render 'form'
