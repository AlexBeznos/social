= content_for :title, t('pages.places.title')
= content_for :navigation, 'false'

.header
  %h1
    = t 'pages.places.my_places'
    (
    %span= @places.length
    )
%ul.facility
  - @places.each do |place|
    %li= link_to place.name, place_path(place)
