%table.table.visits-table
  %thead.thead-default
    %tr
      %th= t(".place")
      %th= t(".visits_count")

  - places.each do |place|
    %tbody
      %tr
        %td= place.name
        %td= place.ahoy_visits.size
