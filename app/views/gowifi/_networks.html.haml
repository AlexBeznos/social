.socials
  - @auths.each do |auth|
    - if Auth::NETWORKS.values.include? auth.resource.class::NAME
      = link_to "#{ENV['APP_URL']}auth/#{network.name}?place=#{place.slug}", class: 'wifi_link' do
        - icon = place.social_network_icons.find_by(social_network_id: network.id)
        = image_tag icon ? icon.icon.url : "wifi/#{place.template}/#{network.name}.png"
