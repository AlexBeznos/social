.socials
  - auths.each do |auth|
    - if auth.network?
      = link_to network_path(place, auth), class: 'wifi_link' do
        - icon = place.social_network_icons.find_by(network_name: auth.name.to_s)
        = image_tag icon ? icon.icon.url : "wifi/#{auth.name}.png"
