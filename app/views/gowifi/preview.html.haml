
!!!
%html(lang="en")
  %head
    %meta(charset="utf-8")
    %meta(http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1")
    %meta(name="viewport" content="width=device-width, initial-scale=1.0")
    %title= content_for?(:title) ? yield(:title) : "GoFriends"
    = favicon_link_tag 'favicon.ico'
    = csrf_meta_tags
    / Le HTML5 shim, for IE6-8 support of HTML elements
    /[if lt IE 9]
    = javascript_include_tag "http://html5shim.googlecode.com/svn/trunk/html5.js"
    = stylesheet_link_tag "https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css", :media => "all"
    = stylesheet_link_tag "gowifi/preview_style", :media => "all"
    = stylesheet_link_tag "gowifi/preview/#{@message.social_network.name}", :media =>  "all"

  %body
    .container
      .vertical-center-row
        .col-md-2-lg-1

        = button_to 'Рассказать!', "#{ENV['APP_URL']}auth/#{@message.social_network.name}", :class => 'btn btn-md btn-default btn-success btn-block'
        = render "gowifi/preview/#{@message.social_network.name}", message: @message
        = button_to 'Нет, спасибо', gowifi_place_path, :class => 'btn btn-md btn-default center-block', method: 'get'